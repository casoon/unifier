---
import config from 'virtual:casoon-pages/config';
import { url } from '../lib/url';

const path = Astro.url.pathname;
const nav = [
  { label: 'Docs', href: url('docs/') },
  ...(config.hasShowcase ? [{ label: 'Showcase', href: url('showcase/') }] : []),
  ...(config.changelog ? [{ label: 'Changelog', href: url('changelog/') }] : []),
];
---

<header class="header">
  <div class="inner">
    <a class="home" href={url()}>
      <span class="name">{config.name}</span>
      {config.version && <span class="version">v{config.version}</span>}
    </a>
    <nav aria-label="Main">
      <ul>
        {
          nav.map((item) => (
            <li>
              <a href={item.href} aria-current={path.startsWith(item.href) ? 'page' : undefined}>
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
      <span class="sep" aria-hidden="true"></span>
      <a href={`https://github.com/${config.repo}`}>GitHub</a>
      <button class="theme" type="button" data-theme-toggle aria-label="Dark theme" aria-pressed="false" hidden>
        <!-- Shows the theme a click switches to: moon while light, sun while dark. -->
        <svg class="icon-moon" viewBox="0 0 24 24" width="16" height="16" aria-hidden="true">
          <path d="M20 14.5A8 8 0 0 1 9.5 4a8 8 0 1 0 10.5 10.5Z" />
        </svg>
        <svg class="icon-sun" viewBox="0 0 24 24" width="16" height="16" aria-hidden="true">
          <circle cx="12" cy="12" r="4" />
          <path d="M12 2.5v2M12 19.5v2M2.5 12h2M19.5 12h2M5.3 5.3l1.4 1.4M17.3 17.3l1.4 1.4M5.3 18.7l1.4-1.4M17.3 6.7l1.4-1.4" />
        </svg>
      </button>
    </nav>
  </div>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 50;
    border-bottom: 1px solid var(--border);
    background: var(--glass);
    backdrop-filter: blur(16px) saturate(1.6);
    -webkit-backdrop-filter: blur(16px) saturate(1.6);
  }
  .inner {
    display: flex;
    align-items: center;
    gap: 20px;
    max-width: var(--max);
    height: var(--header-h);
    margin: 0 auto;
    padding: 0 24px;
  }
  .home {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--heading);
  }
  .home:hover {
    color: var(--heading);
  }
  .name {
    font-size: 15.5px;
    font-weight: 600;
    letter-spacing: -0.01em;
  }
  .version {
    padding: 1px 7px;
    border: 1px solid var(--border);
    border-radius: 999px;
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 12px;
  }
  nav {
    display: flex;
    align-items: center;
    gap: 2px;
    margin-left: auto;
  }
  ul {
    display: flex;
    gap: 2px;
    margin: 0;
    padding: 0;
    list-style: none;
  }
  nav a {
    display: block;
    padding: 7px 12px;
    border-radius: 8px;
    color: var(--text);
    font-size: 14.5px;
  }
  nav a:hover {
    background: var(--bg-deep);
    color: var(--text);
  }
  nav a[aria-current='page'] {
    color: var(--heading);
    font-weight: 600;
  }
  .sep {
    width: 1px;
    height: 20px;
    margin: 0 8px;
    background: var(--border);
  }
  .theme {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    margin-left: 4px;
    border: 1px solid var(--border);
    border-radius: 9px;
    background: none;
    color: var(--text);
    cursor: pointer;
  }
  .theme:hover {
    border-color: var(--border-strong);
    color: var(--heading);
  }
  .theme[hidden] {
    display: none;
  }
  .theme svg {
    fill: none;
    stroke: currentColor;
    stroke-width: 1.75;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  :global([data-theme='light']) .icon-sun,
  :global([data-theme='dark']) .icon-moon {
    display: none;
  }
  /* Narrow screens: navigation moves to a second row. */
  @media (max-width: 640px) {
    :global(:root) {
      --header-h: 100px;
    }
    .inner {
      flex-wrap: wrap;
      gap: 4px 8px;
      height: auto;
      min-height: var(--header-h);
      padding: 10px 16px;
    }
    nav {
      width: 100%;
      margin-left: -8px;
    }
    .sep {
      display: none;
    }
    nav a {
      padding: 7px 8px;
    }
    .theme {
      margin-left: auto;
    }
  }
</style>
